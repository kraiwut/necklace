<%= form_for ["admin", @product] do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.label :price %>
	<%= f.number_field :price, :min => 0 %>

	<%= f.submit %>
<% end %>