<%= content_tag_for :tr, product do %>
	<td>
		<%= link_to product.name, 
									edit_admin_category_product_path(category, product) %>
	</td>
	<td><%= product.price %></td>
	<td>
		<%= link_to "Delete", admin_category_product_path(category, product), 
									:method => :delete, :class => "btn btn-danger" %>
	</td>
<% end %>