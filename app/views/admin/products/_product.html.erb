<%= content_tag_for :tr, product do %>
	<td><%= link_to product.name, edit_admin_product_path(product) %></td>
	<td><%= product.price %></td>
	<td>
		<%= link_to "Delete", admin_product_path(:id => product), 
									:method => :delete, :class => "btn btn-danger" %>
	</td>
<% end %>